# 第三问论文材料交付核验

## 内容口径

正文主方案为21个运输架次加3个中继架次，运输能耗64.095969 kWh，中继能耗2.349304 kWh，总能耗66.445273 kWh，联合完工6612.284 s。22个运输架次加3个中继架次为运输下限对照，能耗66.035704 kWh、联合完工6612.283 s；在相同权重和固定归一化尺度下，其评分高于主方案。

## 结构核验

- DOCX：8幅内嵌图、9张表、79个Office Math对象、10个显示公式、9个重复表头。
- LaTeX：4张正文表、5张附录表、8张PDF图；正文和附录均提供目录版与表格展开版。
- 数据：80个货箱、21个运输架次、3个中继架次、240个连续通信区间；主方案独立核验11884项、0错误。

## 渲染核验

- Word通过注册的WPS导出PDF，共19页，并逐页查看PNG接触页和单页图像。
- LaTeX第三问独立预览通过XeLaTeX，共19页；日志中第三问新增引用未出现undefined或Overfull。
- 完整工程副本已编译，输出 `main.pdf`；其中保留的警告属于原第二问已有的 `zhang2021energy` 引用和旧附录标签，不由第三问材料引入。

## 文件边界

本交付版只新增和读取 `第三问` 目录及独立QA副本，不修改 `E:\sxjm\哒哒哒哒哒` 下载工程，不修改师兄第二问目录，也不覆盖第三问历史求解结果。来源文件SHA-256见 `数据/论文用数据/source_manifest.json`。
