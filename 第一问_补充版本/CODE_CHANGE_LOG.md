# 第一问代码与发布修改记录

仅节选第一问相关条目，保留原项目编号；其他问的修改记录不在本目录发布。

2. 补记此前第一问单点组批求解与结果导出的代码修改（2026-09-23，已完成上一小项）。
   1. 涉及文件：`code/solve_q1_batching.py` 的 `read_inputs`。原来的代码：无该求解文件。修改后的代码：只读原始节点、机型与逐箱工作簿，核验80个唯一箱号、758 kg、2011 L，以及同类货箱属性相同；按四类数量聚合。原因：保证输入来自附件，并为无损数量状态压缩提供前提。回退依据：删除新增脚本及派生输出即可，原始附件不受影响。
   2. 涉及文件：同文件的 `cells_on_segment`、`compute_routes`。原来的代码：无航线DEM计算实现。修改后的代码：以EPSG:32649直线计距，最多5 m分段逆投影到原EPSG:4326栅格，按PixelIsPoint中心注册转换索引并遍历穿越像元，巡航海拔为最高高程加50 m；检查覆盖、NoData和负爬升。原因：避免漏过山脊或错用半像元注册。回退依据：移除新增计算函数，不改原DEM。
   3. 涉及文件：同文件的 `energy`、`safe_payload`、`times`。原来的代码：仅有文字模型，无数值函数。修改后的代码：实现题目3/2次幂航程关系，按已声明补充口径计算去程带货、返程空载的水平与爬升能耗；按安全电量求可行边界，分别累计飞行与作业时间。原因：明确电池已包含在空载总质量中，返程仍有机体爬升能耗。回退依据：删除实现；若官方澄清能耗分项，替换 `energy` 后重算，不能保留旧结果冒充新口径。
   4. 涉及文件：同文件的 `solve_service`、`better`、`main`。原来的代码：无组批优化和具体箱号输出。修改后的代码：完整枚举可行数量组合，字典序DP求最少架次/最低能耗/最短累计作业时间，无放回回填真实箱号，输出JSON、CSV、原始输入SHA-256和计算环境。原因：确保每箱恰好一次、全过程可追溯。回退依据：新增程序及 `results/question1_batching` 为独立文件，不影响原数据。
   5. 涉及文件：`code/export_q1_batching.mjs`。原来的代码：无Excel交付程序。修改后的代码：只读验证原模板9列表头，新建组批、各区汇总和计算明细三表，采用公式计算汇总与余量，重算检查并渲染预览；输出 `outputs/question1_batching/第一问_货箱组批方案.xlsx`。原因：提供完整箱号与可核对结果。回退依据：删除新导出文件及生成物，原模板不变。
   6. 涉及文件：`code/requirements_q1.txt`、`code/README_Q1.md` 与本地运行依赖连接。原来的代码：无本问运行说明。修改后的代码：记录numpy/Pillow/openpyxl/pyproj版本，pyproj安装在项目 `.runtime_py`；通过 `code/node_modules` junction 连接bundled Node依赖，不修改bundled目录。回退依据：只删除项目内新增文件/连接本身，不递归删除连接目标。
   7. 验证依据：18架次、B9/C9、80箱恰好各一次；独立地形完整像元集合一致；原始逐箱位掩码DP与40位Decimal复算结果一致。具体证据在 `results/question1_batching/geometry_audit.json`、`independent_audit.json`。


3. 修改了第一问只保留单一字典序解、不能量化三个指标完整权衡的问题（2026-09-23）。
   1. 涉及文件：新增 `code/solve_q1_multiobjective.py` 的输入与候选生成部分。原来的代码：`solve_q1_batching.py` 仅生成一套 `(N,E,T)` 字典序方案。修改后的代码：新程序核对原始五类输入SHA-256，重新读取工作簿，复用已验证物理函数与航线几何，重新枚举所有732个可行机型—数量组合。原因：保持计算口径一致，同时不覆盖上一小项结果。回退依据：删除新程序与专属输出目录，原求解器与原方案保持不变。
   2. 涉及文件：`Label`、`weakly_dominates`、`nondominated`、`local_frontier`。原来的代码：每个状态只存一个最优三元组。修改后的代码：为每个四类需求状态保留完整非支配 `(N,E,T)` 标签与批次索引，只在同一状态内删除被支配或数值等价标签；能耗比较容差1e-9 kWh，时间1e-6 s。原因：避免有限权重扫描漏掉离散Pareto点。回退依据：原单解DP未改；移除新多目标实现即可。
   3. 涉及文件：`main` 中逐区前沿合并及端点选择。原来的代码：各区各选一个字典序最优值后相加。修改后的代码：对各区前沿进行全部逐项相加并去支配，保存完整全局前沿；分别计算N/E/T、N/T/E、E/N/T、T/N/E四种排序的端点，量化能耗、架次、时间差额及明确归一化后的权重阈值。原因：区别全局最低能耗与最少架次条件下的最低能耗。回退依据：删除专属多目标生成物，不影响原有18架次方案。
   4. 涉及文件：`materialize`、`summarize`、`verify_plan` 及等成本方案回填。原来的代码：无多套完整箱号方案。修改后的代码：为每个目标点恢复完整箱号方案；推荐点沿用原批次，其他点在局部目标相同的服务区复用原具体分组，确保本例只有S008发生调整；逐方案检查80箱各一次、同区、质量、体积、能耗和SOC。原因：保持方案比较可解释，避免无关的同类箱置换掩盖实际变化。回退依据：移除新结果，旧组批JSON与Excel始终未改。
   5. 涉及文件：`code/README_Q1.md`、`results/question1_multiobjective/第一问_多目标优化分析.md`。原来的代码/说明：仅解释单解运行和第一问初始建模。修改后的代码/说明：新增多目标运行入口、完整前沿递推、两个方案数值、S008差异、字典序选择和权重归一化示例，并明确累计作业时间不等于并行任务完工时间。原因：将方法、实际结果及适用口径对应起来。回退依据：删除本次追加节及新增报告，保留原说明。
   6. 验证依据：主程序全部644个数量状态；独立数量DP完整前沿一致；独立真实箱号40位Decimal端点求解一致；另完整穷举S008的1399个可行分割与机型方案确认前沿完整。正式2个目标点、37个批次均通过 `results/question1_multiobjective/validation.json`，每套方案80箱各一次，只有S008改变。两目标点为18/59.131669241559365/32776.09267694372与19/59.03431353209817/34440.065206685635（架次/kWh/s）。


4. 修改了多目标结果缺少可核对 Excel 对照表的问题（2026-09-23）。
   1. 涉及文件：新增 `code/export_q1_multiobjective.mjs` 输入与校验部分。原来的代码：无多目标导出入口。修改后的代码：读取真实 `multiobjective.json`，与旧 `solution.json` 核对基准值，并校验方案ID、前沿内部非支配关系、箱号、质量/体积、能耗四分量、SOC、作业时间、机型计数与总计。原因：避免导出时错把累计时间、方案ID或安全余量混用。回退依据：删除新增脚本与专用输出，原导出程序不变。
   2. 涉及文件：同文件三表写入与公式部分。原来的代码：只有上一小项的单方案Excel。修改后的代码：新建 `方案比较`、`完整前沿`、`代表方案批次` 三表，记录2个前沿点和37个代表批次；通过COUNTIFS、SUMIFS、MIN、VLOOKUP、IF/AND等公式汇总并校验余量。原因：提供完整箱号及两个方案的变化量。回退依据：移除 `outputs/question1_multiobjective` 的新增生成物，旧Excel与原模板保持不变。
   3. 涉及文件：同文件重算与预览部分。原来的代码：无多目标工作簿检查。修改后的代码：重算后与JSON断言比较，导出并生成6张预览，检查比较值、口径说明、前沿、两方案批次和参数。原因：数值与显示均需可核对。回退依据：删除新增脚本和预览，不影响原始输入。
   4. 验证依据：最终Excel独立读取通过，2个目标点与37个批次逐行一致，两方案分别覆盖80箱各一次；37行安全检查全部满足，208个公式及缓存有效，6张预览中文与箱号显示完整。


5. 修改了固定20%返航余量求解无法分析连续载荷变化、离散拆批阈值及任务不可行上限的问题（2026-09-23）。
   1. 涉及文件：新增 `code/solve_q1_reserve_sensitivity.py` 的 `load_data`、`Solver.__init__`。原来的代码：基准求解只保存20%余量下732个可行机型—组合。修改后的代码：核对原始五类文件SHA-256，读取原始工作簿并复用已验证几何，重新枚举742个质量/体积可行组合，记录每个组合 `100*(1-Etrip/Euse)` 的临界余量。原因：降低余量时会增加候选，不能直接复用20%过滤后的集合。回退依据：删除新增脚本和专属结果目录即可，原基准求解器及原始输入没有修改。
   2. 涉及文件：同文件 `Solver.local`、`signature`、`build_plan`。原来的代码：仅求一套固定余量组批或固定20%下的Pareto前沿。修改后的代码：按当前余量过滤候选，统一采用 `(架次,能耗,累计作业时间)` 字典序数量DP，按服务区与实际可行候选集合缓存；每套结果无放回恢复80个真实箱号并复核质量、体积、能耗和箱号覆盖。原因：隔离安全余量影响，防止不同情景改变目标偏好或遗漏重复货箱。回退依据：新增求解入口独立，前两小项的代码与结果不受影响。
   3. 涉及文件：同文件 `payload`、`payload_data`。原来的代码：最大安全载荷只在基准20%计算。修改后的代码：对15区×3机型计算典型余量采样、满载/空载临界值，以及0%—60%每1个百分点的2745曲线点；复用单调二分函数，空载往返不满足时保存null而非0。原因：区分载荷平台、能量限载与航线不可行，避免把连续载荷误当实际货箱可装数量。回退依据：移除新输出，原物理函数没有修改。
   4. 涉及文件：同文件 `main` 的事件扫描与 `compare_signatures`。原来的代码：无连续余量上的组批事件扫描。修改后的代码：按110个不同候选临界值扫描111个基本区间，再按完整批次签名合并为19段，输出18次逐区改变；所有上端等号可行，后续下界为开区间。原因：粗网格会漏掉35.26572116584988%与35.26716681555608%等接近阈值。回退依据：新增扫描及其结果可以独立移除，不改旧基准。
   5. 涉及文件：同文件 `single_box_limits`、`blocked` 和可行上限计算。原来的代码：只验证已选择批次可行，没有判断全部货箱在高余量下能否交付。修改后的代码：按“各必需单箱至少有一种机型可行”的必要充分条件计算35.26716681555608%上限及S008饮用水瓶颈；不可行情景保存null目标与具体受阻需求，不输出伪造0架次。原因：区分换机/拆批可修复和任何方案均不可行。回退依据：删除新增分析，不影响前两小项结果。
   6. 涉及文件：`code/README_Q1.md` 与新增 `results/question1_reserve_sensitivity/第一问_返航安全余量敏感性分析.md`。原来的代码/说明：仅解释前两小项。修改后的代码/说明：追加敏感性运行和导出命令，记录统一余量、补充能耗口径、最大载荷模型、典型情景、19段区间、等号规则、全局上限及字典序能耗可局部下降的实例。原因：将理论、可复现代码与计算结果一一对应。回退依据：移除本次追加说明及新增报告，保留原章节。
   7. 验证依据：`validation.json` 的独立40位Decimal逐箱bitmask DP核验742候选、111基本区间中点/上端等号/刚超过端点，以及642条实际输出批次记录，全部通过。`payload_validation.json` 独立比对45组双阈值、742批次临界、2745曲线点及270共同采样点，通过且最大载荷差8.76×10⁻⁸ kg。20%基准18/59.131669241559365/32776.09267694372（架次/kWh/s）精确复现。


6. 修改了安全余量敏感性结果缺少可筛选工作簿、机型载荷图和临界组批对照的问题（2026-09-23）。
   1. 涉及文件：新增 `code/export_q1_reserve_sensitivity.mjs` 的输入标准化、`validateScenario` 和基准检查。原来的代码：无余量敏感性专用导出程序。修改后的代码：只读正式 `sensitivity.json` 和旧基准，逐个校验典型情景及区间代表的真实箱号、四类数量、质量、体积、能耗分项、SOC、累计时间与总计；不可行情景必须带具体受阻需求并保持目标空白。原因：避免把0架次误当不可行的运输结果，或在导出时混淆情景余量。回退依据：移除新增导出脚本和专属输出，原始模板与前两轮Excel保持不变。
   2. 涉及文件：同文件工作表与公式生成部分。原来的代码：仅有基准及多目标对照工作簿。修改后的代码：生成 `情景比较`、`最大安全载荷`、`组批变化临界点`、`情景批次`、`口径说明` 五张表，包含16情景、45组载荷、2745曲线点、19区间与18转换点，保留12套可行情景的237条真实箱号批次；通过公式复算汇总、能量预算、SOC、安全检查、单位换算及变化量。原因：用户可以逐行追查模型变化与实际货箱，且临界等号语义明确。回退依据：删除专用Excel与导出脚本，不改输入数据。
   3. 涉及文件：同文件 `nativeChart`、`compareCharts`、`payloadCharts`。原来的代码：无敏感性原生图表。修改后的代码：三个运输指标分开使用典型情景分类柱状图，避免非均匀余量被误画为连续等距横轴；S001/S004/S008使用0%—60%每1个百分点的均匀网格绘制A/B/C载荷折线，共六张原生Excel图表，空载不可行处保留空白、曲线终止。原因：区别连续载荷与离散方案变化，同时不混合不同单位。回退依据：图表和辅助公式均位于新工作簿，删除新增导出文件即可。
   4. 涉及文件：同文件重算、断言、错误扫描和预览导出部分。原来的代码：无此工作簿的数值与显示核验。修改后的代码：重算后按正式JSON核对可行情景目标，断言237批次安全检查满足、不可行情景目标为空、六图绑定原生源单元格；扫描公式错误并生成九张PNG预览，用于检查表头、箱号、说明、载荷曲线和临界变化的显示。原因：保证文件既可计算核对，也能正常阅读。回退依据：移除新预览和工作簿即可。
   5. 验证依据：最终 `第一问_安全余量敏感性.xlsx` 为129598字节，5表、6原生图、237批次、450典型载荷值及2745曲线值全部与正式JSON对应；19区间、18变化逐项一致。2134个公式中1999个数值/文本缓存有效，135个合法空缓存对应不可行载荷曲线点，错误单元格0个。导出XLSX图表XML确认3个column图与3个line图、源单元格引用有效；9张预览已检查，中文、箱号与临界值显示清晰。


7. 修改了第一问采用 UTM 坐标、需按用户要求统一为师兄局部椭球平面的问题（2026-09-24）。
   1. 涉及文件：`code/solve_q1_batching.py` 的新增 `local_plane` 及 `compute_routes` 距离部分。原来的代码：`Transformer.from_crs(4326,32649,always_xy=True)` 将节点投影到 UTM 49N 后计算欧氏距离。修改后的代码：以 O01（109.2308517°E、23.0085095°N）为固定原点，取 WGS84 `a=6378137`、`e2=6.6943799901413165e-3`，计算 `N0=a/sqrt(1-e2*sin(phi0)^2)`、`M0=a*(1-e2)/(1-e2*sin(phi0)^2)^1.5`，统一用 `x=N0*cos(phi0)*radians(lon-lon0)`、`y=M0*radians(lat-lat0)` 及 `hypot(x,y)` 计距。原因：按用户决定统一师兄的局部一阶椭球近似。回退依据：旧代码及输出已保存于 `archives/question1_utm_before_local_plane_20260924.zip`，恢复归档中同名文件即可；归档含55份文件与SHA256清单，已检查ZIP完整性。
   2. 涉及文件：同文件 `compute_routes` 的 DEM 遍历与元数据。原来的代码：UTM 直线以最长5 m分段后逆投影，逐段收集相交像元；保存 `distance_crs=EPSG:32649`、`curve_segment_max_m`。修改后的代码：固定局部线性变换的直线对应经纬度直线，直接以两端点进行完整 `cells_on_segment` 求交；继续使用 PixelIsPoint `+0.5`、边界/角点接触即纳入、最高地形加50 m。元数据保存局部原点、椭球、两个曲率半径、尺度系数及16个节点x/y坐标。原因：使距离与取高程使用同一航线，不再需要近似逆投影分段。回退依据：恢复归档中的函数及JSON，不能仅恢复距离而保留新版像元集合。
   3. 涉及文件：同文件参数、物理常数及 `code/requirements_q1.txt`。原来的代码：强制导入 `pyproj/Transformer`，命令行接受 `--terrain-step-m`，爬升项内直接使用9.81。修改后的代码：移除 `pyproj` 运行依赖和不再适用的步长参数，将同一9.81提为 `GRAVITY_M_S2`，并保存 `physical_parameters.gravity_m_s2` 与 `solver_sha256`。原因：局部直线解析求交无需投影库/步长；本轮只统一坐标，重力仍保留原口径。题源核查确认题目未强制投影或重力数值，详见 `results/question1_local_plane/source_rules_check.json`。回退依据：恢复归档中的主程序和依赖文件；旧 `.runtime_py` 并未删除。
   4. 涉及文件：`code/solve_q1_multiobjective.py` 的 `main`、`code/solve_q1_reserve_sensitivity.py` 的 `load_data`。原来的代码：只检查原始附件指纹及 `all_cell_sets_equal`。修改后的代码：追加几何审核 `solution_sha256` 与当前基准文件绑定、当前坐标模型检查、`solver_sha256` 与基础物理程序绑定。原因：切换坐标后旧UTM审核不得继续作为新解的通过证据，后续改物理程序也必须重算。回退依据：恢复归档中两文件；对应旧输出和审核必须成套恢复。
   5. 涉及文件：新增 `code/verify_q1_local_plane.py`。原来的代码：项目只有前轮审核JSON，没有可运行的持久独立审核脚本。修改后的代码：不导入主求解器，直接读原始工作簿/DEM，采用闭像元矩形线段裁剪独立核验几何；用40位Decimal计算成本，以真实箱号bitmask DP验证基准与完整Pareto前沿，再以独立数量状态DP审核敏感性区间、端点和实际箱号方案，二分复算载荷。支持先 `--geometry-only` 后完整核验。原因：为新坐标提供可复现独立证据，替换旧审计。回退依据：移除新增脚本、恢复归档旧审核；若继续使用局部坐标则不得以旧UTM审核替代。
   6. 涉及文件：`solve_q1_batching.py` 的CSV输出及三个 `export_q1_*.mjs` 的口径说明。原来的代码：只输出组批CSV，工作簿未注明新局部坐标。修改后的代码：新增 `Q1_局部椭球距离与航线.csv`（15路线）与 `Q1_局部椭球有向航段.csv`（30方向，start/end/distance/cruise_altitude/climb/descent，长度均m）；三个工作簿在既有说明行注明O01局部椭球平面，沿用原格式、公式、工作表和图表重新导出。原因：便于直接共享航线数据，并保持展示与模型一致。回退依据：恢复归档中的导出脚本与旧工作簿，移除新增两CSV即可。
   7. 涉及文件：`第一问_模型建立与求解方法.md`、`code/README_Q1.md`、三个结果目录下的中文分析文档，以及两份旧方案比较文档。原来的代码/说明：介绍UTM航线、旧能耗/时间/阈值及上一轮审核方法。修改后的代码/说明：补齐局部椭球公式、弧度/米单位、g=9.81、运行依赖与审核顺序；按新JSON更新18批、2前沿点、19区间及载荷表；核验范围按本轮实际执行修订，删除未重做的旧穷举次数等。两份历史比较只在顶部标明其数值为改版前UTM口径。原因：防止代码、论文说明和表格混用不同坐标结果。回退依据：前5份恢复归档同名文件；历史比较仅删除本轮顶部说明即可恢复旧内容。
   8. 数据与版本说明：节点、机型、逐箱货物、DEM四个数值输入SHA256与旧版一致。当前题目DOCX指纹为 `f4ca76b46a8dd760bf13d606ec7673656ac9b882a4b54ddd29ab4377da91aa45`，与旧记录 `0a4f507b91a02a0ea4264973dd432cdcb5b8916c61c24214719665f326d57685` 不同；本轮未修改该原题文件，已重新读取当前题目含OMML并核查规则，未据此推断其历史变更原因。新结果记录实际当前指纹。差异证据保存在 `results/question1_local_plane/coordinate_change_comparison.json`。
   9. 验证依据：15条距离与先前独立复现的师兄距离逐值一致；与UTM最大差0.6919363436 m，各线最高地形和爬升相同，6条边缘像元集合改变。新独立几何核验15路线/3554次像元访问全部一致。80箱恰好一次，具体18批箱号与机型不变，B/C各9架次；新总能耗59.13313308089387 kWh、累计时间32776.5167822769 s、最低SOC23.088493096568953%。完整前沿仍2点，另一点19/59.03577746310043/34440.48875202747；敏感性742候选、111基本区间、112端点、19方案段、642批次记录、3195载荷记录通过，全任务上限35.26719978564765%。三个Excel重新计算与错误扫描通过，保存后按JSON核对关键数值，11表渲染检查通过，敏感性6个原生图源引用有效；保存文件证据见 `results/question1_local_plane/workbook_validation.json`。


9. 修改了当前第一问对照对象易混淆、深入分析保留要求未集中记录的问题（2026-09-24，仅文档与新增核查证据）。
   1. 涉及文件：`code/README_Q1.md` 末尾。原来的代码/说明：已介绍完整Pareto与精确余量算法，但未专门记载用户要求持续保留这些深入分析。修改后的代码/说明：追加“已完成深入分析的保留要求”，列出完整前沿、742候选及事件扫描、19段完整箱号方案、载荷曲线、单箱瓶颈和独立审核；明确2点/19段是当前数据结果，参数变化后保留方法并完整重算。原因：后续借鉴其他版本或压缩论文正文时保持已完成分析的完整性。回退依据：仅删除该新增小节即可；求解程序、JSON正式结果和Excel未改。
   2. 涉及文件：新增 `results/question1_current_comparison/第一问_当前版本与师兄及111对照.md` 与同目录核查JSON。原来的代码/说明：只有改版前UTM与师兄、Agent“第一版”的历史对照。修改后的代码/说明：单独比较当前局部椭球版、师兄及真正的 `Agent/111`，记录g/数值设置/目标完整性/余量覆盖差别，核实111实际调用的线性航程、插值地形与单次带权MILP，给出保持111几何只改变航程指数的S004控制复算。原因：避免把此前“第一版”的结论误用于不同目录111，也避免将物理公式差异说成小数精度差异。回退依据：删除本次新增对照目录即可，历史报告未改。
   3. 验证依据：本轮直接读取当前结果，确认19段分别覆盖80唯一箱号、18次转换、2个完整Pareto点、45组载荷和2745曲线点；检查所有区间连续和端点规则，以及多目标/敏感性对当前基准的SHA256绑定。师兄对照与111复算证据均独立保存。没有删减或重写任何求解算法，没有将精确扫描改为粗网格抽样，也没有覆盖旧UTM归档。

10. 修改了各问成果目录分散、第一问缺少统一论文文字、数据与独立论文图组的问题（2026-09-24）。
   1. 涉及文件：新增根目录 `工作区目录说明.md`、第一问至第四问的 `README.md` 和五类子目录。原来的代码/组织：求解器、输出、文稿分布在根目录 `code/results/outputs`，没有按题目组织的阅读入口。修改后的代码/组织：每个大问设“代码、图片、文字、数据、结果”；第一问三个小项仍全部归入第一问，第二和第四问标为待整理，第三问只关联现有成果。原求解文件仍保留原位置；第一问三个正式结果与第三问两个既有目录通过 Windows junction 提供实时入口，未移动原文件。原因：提高可读性并维持第三问公共模块导入与原运行路径。回退依据：删除本次新增目录文件和联接本身即可；不得递归删除联接指向的原目录。
   2. 涉及文件：新增 `第一问/代码/复现第一问.py`、代码 `README.md`、`requirements_paper.txt`。原来的代码：没有新题目录下的复现入口。修改后的代码：通过 `subprocess.run([sys.executable, 原脚本], cwd=ROOT, check=True)` 调用原求解器或材料脚本；默认 `--step materials` 只整理既有结果，重算三个小项需要明确选择对应参数。追加 matplotlib3.10.8、contourpy1.3.3、cycler0.12.1、fonttools4.60.2、kiwisolver1.4.9、pyparsing3.2.5、pypdf6.10.0 依赖说明；新安装包隔离在 `.runtime_py`，未升级或替换numpy，未改第三问 `.runtime_q3`。原因：使新目录可执行，又不改已验证核心算法。回退依据：删除新增入口和增量依赖说明；如移除新依赖，只移除本次明确新增的包，不删除既有 `.runtime_py` 内容。
   3. 涉及文件：新增 `第一问/代码/prepare_q1_paper_data.py` 的来源绑定、无损CSV导出及回读部分。原来的代码：第一问结果主要为JSON和Excel，没有统一字段字典及可绘图长表。修改后的代码：从正式JSON导出15张主要CSV和字段字典，逐单元格回读验证；记录原始来源SHA256及依赖绑定，保留数组为JSON、不可行为null、浮点数完整精度。原因：保证论文数值可追溯，不因显示舍入或缺失补零失真。回退依据：删除新增脚本、数据CSV和data_manifest即可；原附件及正式JSON保持不变。
   4. 涉及文件：新增 `第一问/代码/plot_q1_geography.py` 的地图与地形剖面部分。原来的代码：没有可直接用于论文的真实DEM航线图及保守剖面图。修改后的代码：F01叠加原始DEM、16节点、15航线和局部米制坐标；F02解析每个原始闭像元的相交区间，绘制分段常值保守上包络，保留孤立角点高峰、节点作业海拔、双向爬升与最高地形加50m巡航线；输出剖面源CSV及3种格式。原因：使地理图与正式模型的最高地形规则一致，避免插值平滑替代真实像元约束。回退依据：删除脚本及F01/F02新增图/源表即可，未修改原DEM或compute_routes。
   5. 涉及文件：新增 `第一问/代码/make_q1_paper_figures.py` 的F03–F16和源数据输出。原来的代码：没有独立论文图组，已有图片以Excel预览为主。修改后的代码：直接读取当前三个正式JSON，生成需求、批次组成、约束利用率、能耗/时间分解、安全载荷、完整Pareto、S008拆批、精确余量阶梯、19段指标、45组载荷曲线、最后窄区间、满/空载阈值及求解流程共14幅图；输出600dpi PNG、文字SVG、嵌入字体PDF与逐图源CSV。绘图前先记录图形结论、依据、单位和误读风险。原因：形成可用于论文且能回查真实数值的图件。回退依据：删除新增脚本及F03–F16生成物；求解结果不受影响。
   6. 涉及文件：同绘图脚本 `exact_steps` 与 `f15` 的本轮视觉修正。原来的代码：每段都画下端空心点；满/空载余量图横轴下界为0。修改后的代码：仅在前后目标值不同时画新段空心下端，避免覆盖旧段相同数值的实心上端；横轴下界扩到-5%，明确标注S008/C型满载阈值-0.475689%并解释其表示0%余量下仍不能额定满载。原因：正确表达精确端点归属及负公式边界。回退依据：可恢复上述两处旧绘图表达，但这会重新引入展示歧义；底层数值始终未改。
   7. 涉及文件：新增 `第一问/代码/build_paper_package.py`。原来的代码：没有统一图册、图注清单、可分享材料包和导出核验。修改后的代码：合并16幅矢量PDF为带首页和书签的17页图册，生成4张缩略索引和图注/选图建议；按正式来源SHA256复制3份工作簿、1份DEM、题目规则和3份已验证Excel；检验PNG分辨率、PDF可提取文字/字体、SVG文字和Markdown链接；打包真实源码、输入、正式结果与核验文件，遍历显式跳过联接和依赖目录，逐条核对ZIP内SHA256。原因：便于写论文与给队友分享，同时避免联接导致递归重复。回退依据：删除新增脚本、汇编生成物、交付副本与本次ZIP即可；原文件不变。
   8. 涉及文件：上述汇编脚本 `verify` 与新增 `第一问/结果/图形人工审阅.json`。原来的代码：导出脚本可能直接写固定“已目视通过”文字。修改后的代码：将本轮实际审阅的16张PNG指纹单独记录，汇编时按指纹判断当前图是否仍对应已完成目视检查；图变更后提示重新人工检查，不把自动重导出当新目视审核。原因：区分结构检查和实际视觉证据。回退依据：删除记录及该检查，不影响绘图和模型，但会失去版本化视觉证据。
   9. 涉及文件：新增 `第一问/文字/第一问_论文正文.md`、`写作说明.md`、`数据来源与处理说明.md`、`图表设计约定.md`、生成的图表选用说明。原来的代码/文字：分析散落于三个结果目录，未形成统一论文稿。修改后的代码/文字：按问题分析、准备、建立、求解、结果及适用范围组织约6500中文字，20个公式块，18批摘要、2个Pareto点、19段完整精确区间表及6幅主图引用；各数值逐项与JSON一致，不加入未经核实文献或实测声明。原因：直接支持论文写作并保留所有深入分析。回退依据：删去新增论文材料即可，早期模型文档与历史归档均保留。
   10. 验证依据：80箱/758kg/2.011m³与18批逐项一致；数据表完整精度回读通过，原输入和Excel副本SHA256一致；16幅图全部实际查看，另独立检查地理图和F11–F15，修正上述两处问题；PNG均600dpi，SVG保留文字，单图PDF均单页且可提取文字，无Type3字体；17页图册及文件链接、ZIP CRC和逐文件哈希通过。原18架次、2点前沿、110内部阈值/111基本区间/19最优段/18转换、45组载荷/2745样点、35.26719978564765%可行上限及原独立审核材料全部保留。未重算或修改第三问。
   11. 涉及文件：`复现第一问.py` 的控制台输出与 `build_paper_package.py` 的地理图审核同步。原来的代码：默认Windows编码可能使中文运行提示乱码；地理图重导出会将本轮视觉状态重置为pending。修改后的代码：父进程UTF-8输出、子进程传递 `PYTHONIOENCODING=utf-8`；仅在新PNG与已目视审阅指纹一致时，将地理图状态记为 `passed_by_matching_reviewed_png` 并链接原审阅记录，否则保持pending。原因：保证用户可读输出且审核状态不与真实证据矛盾。回退依据：去除编码设置和该元数据同步；不会修改模型或数据。复现入口 `--step materials` 已完整运行成功，四个材料阶段依次完成，重导PNG与16幅已审阅图指纹全部一致。


11. 修改了第一问与师兄当前版本的数值细节不一致、补充比较范围未更新、论文交付仍绑定旧参数的问题（2026-09-24）。本次仅统一第一问；师兄提交为 `9d1bddc2636a38a2f0445437420521389ad2fa49`，目录只读。变更前已将195个文件及SHA256清单保存至 `archives/question1_before_peer_alignment_20260924.zip` 并通过CRC验证；恢复时应成套恢复对应源码、JSON、核验、表图与正文，避免混用新旧参数。该归档不被新论文材料包覆盖。
   1. 涉及文件：`code/solve_q1_batching.py` 的常量、`energy`、`safe_payload` 与结果元数据。原来的代码：`GRAVITY_M_S2=9.81`、`EPS_ENERGY=1e-10`、`EPS_TIME=1e-7`，载荷二分直接用 `1e-7 kg`。修改后的代码：分别采用 `9.80665`、`1e-9 kWh`、`1e-6 s`，新增 `PAYLOAD_TOLERANCE_KG=1e-6` 并用于二分；假设、物理参数和数值容差一并输出。原因：按用户要求将不改变基准分组的常规设置向师兄统一；原题没有指定g数值，不能表述为题给常数。回退依据：从变更前ZIP恢复同名脚本和配套正式结果；载荷二分精度只控制连续上限报告，整箱可行性仍直接检验能量约束。
   2. 涉及文件：同脚本 `batch_cost`、`better`、`solve_service`。原来的代码：累计浮点能耗/时间，再用显式容差比较，候选按原机型枚举次序遍历。修改后的代码：新增 `ENERGY_COST_SCALE=10**12`、`TIME_COST_SCALE=10**6`；每个候选先 `round(E*scale)` 和 `round(T*scale)`，DP累加整数三元组并作字典序比较；候选按 `(counts, model)` 稳定排序，报告用 `math.fsum` 累计未量化原值。原因：严格对应师兄逐候选量化规则，不能用“累计浮点后再round”替代；同目标恢复次序一致。回退依据：恢复旧 `better`、候选顺序与浮点DP；不要将旧容差比较与新整数量化混合使用。
   3. 涉及文件：`code/solve_q1_multiobjective.py` 的 `Label`、`weakly_dominates`、`nondominated`、局部前沿及全局合并。原来的代码：Label只存浮点成本和批次索引，以浮点容差判断支配。修改后的代码：保留原成本，同时新增 `energy_key/time_key`，逐候选量化并累加，按整数键精确去支配；全局合并也累加整数键，输出仍报告原始能耗/时间，并记录比较定义和尺度。原因：统一所有目标比较口径，同时保留完整非支配DP，避免退化成少数目标排序求解。回退依据：从ZIP恢复本文件及多目标结果，原独立高精度审核也应恢复对应版本。
   4. 涉及文件：同多目标程序端点映射及 `code/verify_q1_local_plane.py` 的独立端点核验。原来的代码：仅列N/E/T、N/T/E、E/N/T、T/N/E四种排序。修改后的代码：增加E/T/N和T/E/N，保存全部六种完整排列；独立审核逐一核验映射并保存 `six_scalar_endpoint_mappings_verified=true`、`endpoint_mapping_count=6`。原因：师兄最新版已补齐六排序，需要纠正此前只基于旧三排序快照的比较；六个端点不等于一般意义的完整前沿。回退依据：恢复旧四排序代码与结果映射；完整前沿算法不需删除。
   5. 涉及文件：`code/solve_q1_reserve_sensitivity.py` 的候选排序、数量DP与报告。原来的代码：每状态累计浮点目标，典型情景缺5%/45%，载荷采样10档。修改后的代码：使用统一候选顺序和整数DP，另存 `integer_objective`，物理原值继续报告；补5%与45%，形成18个报告情景（含精确上限）与12档载荷样本，注明5%—45%每5个百分点的九档常规对照。原因：覆盖师兄相同情景而不删原有补充情景。回退依据：恢复旧情景/样本常量和DP实现；对应CSV、Excel和情景数也须一并恢复。
   6. 涉及文件：同敏感性程序精确事件模块。原来的代码：以 `100*(1-Etrip/Euse)` 定义批次临界值并扫描，`RHO_TOL=1e-10` 个百分点。修改后的代码：本部分公式与边界容差继续保留，只由新g重新计算能耗与事件位置，仍742候选、110内部临界、111基本区间、19段与18转换。原因：常规能量浮点容差不代表额外可用电量，不能借本次数值统一平移物理上限或删掉用户要求保留的精确扫描。回退依据：恢复旧参数后完整重算事件，不可只改显示端点。新旧19段的对应分组完全一致，但端点间极窄窗口内固定同一余量时的选择可能不同。
   7. 涉及文件：`code/verify_q1_local_plane.py` 的独立Decimal物理量和载荷误差检查。原来的代码：独立g取 `Decimal('9.81')`，连续载荷差断言 `1.1e-7 kg`。修改后的代码：取 `Decimal('9.80665')`、载荷允许差 `1.1e-6 kg`；仍重读附件、独立实现几何和40位Decimal能耗，不导入主求解器来代替独立审核。原因：审核必须检验同一物理参数和已声明的二分精度。回退依据：恢复ZIP中的审核文件和其对应验证JSON。完整审核已实际重新执行，包含六端点、完整前沿、所有余量区间与载荷记录。
   8. 涉及文件：`code/export_q1_batching.mjs`。原来的代码：说明硬编码g=9.81、旧统一误差文字，说明预览固定8行。修改后的代码：断言并读取基准JSON的g和容差，解释逐候选整数比较与原值报告，预览范围按 `notes.length` 自动扩展。原因：工作簿说明必须与计算来源一致且不裁掉新增说明。回退依据：恢复旧导出器与归档Excel；原题结果模板未修改。
   9. 涉及文件：`code/export_q1_multiobjective.mjs`。原来的代码：只展示两个前沿方案，缺六排序映射表；g和说明预览范围固定。修改后的代码：在“方案比较”A13:G19加入六排序表，用INDEX/MATCH引用“完整前沿”及公式换算小时，对六行重算值与JSON断言；注释区后移避免覆盖，说明/预览范围动态计算，g与整数尺度从当前来源读取，新增排序预览。原因：把师兄已补充的目标优先顺序直接呈现，同时保持两个完整前沿点及37条方案批次。回退依据：恢复旧导出器和工作簿，删除新增优先顺序预览即可。
   10. 涉及文件：`code/export_q1_reserve_sensitivity.mjs`。原来的代码：载荷表断言10档样本，固定A:O范围和旧g说明。修改后的代码：验证样本非空且唯一，新增列号转换，表头、数据、格式、条纹与预览按样本数量计算，当前12档至Q列；读取新g/容差，保留6个可编辑图表，情景批次随18情景动态更新。原因：新增5%/45%后避免列数冲突、遗漏数据或裁切。回退依据：恢复旧脚本与旧16情景/10档结果整套文件；不改原模板。
   11. 涉及文件：`第一问/代码/prepare_q1_paper_data.py`、`make_q1_paper_figures.py`、`build_paper_package.py`。原来的代码：数据说明g、情景数及部分图内能耗/阈值/区间宽为上一版固定文字，F14放大范围固定；材料包未纳入本次比较证据。修改后的代码：g取 `baseline['physical_parameters']`，情景数取实际长度；F06/F09/F11/F14/F15和图册首页的能耗/边界/跳变值动态读正式JSON，F14横轴随末区间宽度缩放；打包纳入 `results/question1_peer_alignment`。原因：参数统一后所有表图必须同步，后续重算无需手工更换临界数字。回退依据：恢复三脚本及归档中的论文材料；完整图注由builder生成，不能只改生成文件。
   12. 涉及文件：`第一问/代码/复现第一问.py`、本目录代码README与题目README。原来的代码：基准重算后直接运行Pareto/敏感性，旧几何审核仍绑定旧基准hash，后续依赖检查会失败。修改后的代码：基准完成后自动调用独立审核 `--geometry-only` 再继续；README解释此依赖、完整审核/Excel更新次序，并加入版本对照入口。原因：修复重算入口依赖顺序而不跳过来源校验。回退依据：去掉新增子进程调用，改为手工先运行几何审核；其余材料步骤不变。
   13. 涉及文件：新增 `results/question1_peer_alignment/audit_q1_numeric_alignment.py` 与 `第一问/代码/compare_q1_peer.py`。原来的代码：无本次参数统一的可重放预演和正式结果对照。修改后的代码：预演禁写pycache，只读师兄源码/API及本地同内容附件，隔离g与比较规则；正式核验对照师兄六排序、九情景、15航线和45组载荷，并逐箱比较变更前归档中的基准与19段，保存来源SHA256、版本号和变化量。原因：用实际逐箱证据判断“细节不改变组批”，不只比较总架次。回退依据：删除新增脚本和peer_alignment目录；脚本未写师兄目录。重放仅覆盖师兄纯计算API，90组MILP是核对保存证据及源码hash，本轮未重跑。
   14. 涉及文件：第一问8份正式文字、新 `与师兄版本的差异及统一说明.md`、3份历史对比顶部标记、CSV/图注/16图/图册及交付副本。原来的代码/说明：当前材料混有旧g、四排序与旧能耗阈值；历史比较未突出版本。修改后的代码/说明：正文和模型说明按新JSON同步18批、2点、六顺序、18情景、19段；历史报告保留旧证据并标明历史；差异说明区分“对应段分组不变”和“区间端点移动”，并说明文献原文缺失和不同题目不可直接比较。原因：使论文论断、公式、数据和图表相互一致，不迁移师兄的阅读全文履历。回退依据：文档、图片和结果均有变更前ZIP，不覆盖原题、附件和旧UTM档案；第三问源码、环境与正式结果没有本轮修改。
   15. 验证与回退依据：基准仍80箱、758 kg、2.011 m³、18架次B9/C9，能耗59.13226032599277 kWh、累计32776.5167822769 s、最低SOC23.089248385317727%；完整前沿仍2点，第二点19架次/59.03487773780201 kWh/34440.48875202747 s。19段逐箱/机型同旧版，全任务上限更新为35.26764734919061%。独立15路线/3554像元访问、742候选、111区间/112端点、660批次、3285载荷记录通过，载荷最大差8.939059625845402e-7 kg。7幅发生变化的图F05/F09/F11/F12/F13/F14/F15重新目视通过，9幅PNG指纹不变继承原审核，记录保存全部新hash。三Excel重导并由独立回读核查，公式数分别360/232/2236，错误均0；六排序、18情景/13可行/255批次、45×12样本、2745曲线点、19区间与6原生图表全部通过，9张受影响工作簿预览已实际查看；没有进行Excel桌面应用实测。完整证据见 `results/question1_peer_alignment/workbook_validation.json`；最终材料格式/链接/副本/ZIP核查见 `第一问/结果/材料清单与核验.json` 和材料包内清单。


13. 修改了第一问成果尚未以独立目录供GitHub团队论文写作参考的问题（2026-09-24，仅发布组织，不改求解模型）。
   1. 涉及文件：新增 `第一问/代码/prepare_q1_github_upload.py` 的归档筛选与目录创建。原来的代码：只有含原始附件的本地论文材料ZIP，没有针对师兄仓库的独立发布流程。修改后的代码：从已核验ZIP提取第一问代码、文字、图、派生数据、Excel及结果到新目录 `第一问_补充版本`，保留内部相对结构；排除原题原附件、旧包清单及总项目日志，已有目标目录时拒绝覆盖。原因：按用户要求只添加我们的第一问，保持师兄所有已有文件。回退依据：本次为全新目录，可按发布提交整体revert；原材料ZIP和数学建模工作区成果保持原样。
   2. 涉及文件：同脚本生成README、独立.gitignore、第一问日志节选及Markdown链接适配。原来的代码/说明：存在本机绝对链接、未随包上传的outputs Excel链接、依赖Windows联接和原附件副本的说明。修改后的代码/说明：新增论文阅读导航、输入准备和依赖说明；Excel链接映射到已发布的第一问/结果，项目链接改相对路径；本机Python/Node示例改为通用命令；只节选第一问日志，不复制其他问记录。原因：使师弟可以直接在线阅读，同时不误称未上传原件已包含。回退依据：改动仅作用于上传副本，源文稿和已验证计算文件未修改。
   3. 涉及文件：同脚本的SHA256清单、范围检查与 `results/question1_github_upload/pre_upload_validation.json`。原来的代码：无发布前文件级保护。修改后的代码：保存师兄所有已跟踪文件SHA256，创建新目录后逐个复核不变；核对Python/MJS/JSON/CSV/Excel/图片等与已验证源包原字节相同，检查全部Markdown链接及文件大小，输出独立UPLOAD_MANIFEST。原因：不重新求解仍能确保发布内容对应已验证结果，避免捎带无关修改。回退依据：移除新增上传工具与其专属核验记录；发布状态以最后Git远程提交核验为准，准备成功不代表已推送。
