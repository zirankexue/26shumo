# 远端搜索：试验记录，未采用为正式方案

这里的 `solution_recommended.json` 是本次远端试验的候选，总能耗 66.44527328256846 kWh、联合完工 6612.283 s，对应远端及本地复验均为 11902 项。它未替换论文正式方案；正文最终结果请使用[正式方案](../../solution_recommended.json)及[写作统一口径](../../../../第三问_写作统一口径.md)。

`solution_fixed_refinement.json` 与 `solution_search_best.json` 字节完全相同，分别是固定结构精修结果和当时最佳结果快照。它们是同一次运行的两个保存入口，没有形成两份并列的正式答案。`solution_recommended.json` 另带箱号等价调整和搜索历史，不能仅凭文件名与前两者混用。
