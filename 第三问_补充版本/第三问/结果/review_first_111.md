# 第一版与111：第三问源码复核及当前21+3方案升级判断

本轮重新读取了两份第三问实际源码、入口、公共物理函数、运行日志和保存结果，并与当前师兄版Q3的实际调用链比较。未导入或运行旧求解器，未修改 `D:/Desktop/Agent`、现有solver或正式方案。证据见 [review_first_111_evidence.json](./review_first_111_evidence.json)，其中冻结17份来源文件SHA256、18项代码片段和行号。旧 `four_agent_evidence.json` 没有源文件哈希字段，因此不能证明旧轮与本轮源码字节完全相同；本轮结论以重新读取的源码为准。

下文“当前方案/已吸收”指冻结21+3基线。收尾复核时，父任务正在并行升级代码：solver已出现多`requirements`窗口关联接口，validator已支持声明`communication_policy.max_relay_stations_per_sortie`；默认`Profiles`仍生成单站选项。JSON同时记录了审查初、末源码哈希。本报告不把尚在验证的新接口当作已取得的改进结果。

当前对照解为 `solution_recommended.json`，SHA256为 `35e203d5426de54543065ac5051a8956e6450b1092e96bb9ed1853fb3b653bf2`：80箱，21运输＋3中继，66.841159703975 kWh，6620.617 s，0迟到，6个多服务区运输架次。当前目标仍是冻结归一化尺度下的3:1.5:1:0.5，不能用旧方案的不同目标或不同能耗口径直接比较数值。

## 1. 结论与优先级

最值得借鉴的是**任务驱动的候选补点**与**同一运输架次的分段多站后备**。第一版的固定结构LP可作为快速内层或数值诊断，但当前方案已有更广的固定候选CP-SAT精修，实测再做同结构LP只有极小变化。单独增加同站多次出勤或能源组件复用，对现21+3方案暂没有资源瓶颈依据。紧急运输共享中继已经实现，应提升候选筛选和联合时序，不能把“共享”本身重复包装成新增功能。

| 可借鉴方法 | 旧代码是否实际实现 | 当前吸收情况 | 当前建议 |
|---|---|---|---|
| 固定结构LP重排 | 第一版主入口实际调用，日志显示HiGHS成功 | 当前固定结构CP-SAT已优化箱序、站点及时间 | 保留作快速内层/小规模下界诊断 |
| 同站多次出勤 | 第一版正式保存结果中站0、站1各出勤两次 | 当前每候选站仅一次任务 | 低优先级；现有服务并集没有大空隙 |
| 中继能源组件充电后复用 | 两版均有分配逻辑；111保存结果实际复用 | 当前每次中继用不同初始满电组件 | 现3次任务、6组件，单独开放复用不会解除瓶颈 |
| 多运输共享同一中继 | 111早期任务分组，第一版按区间共同覆盖 | 当前三个中继已并发保障2/4/7架运输 | 已吸收；可增加紧急任务覆盖掩码辅助候选选择 |
| 一个运输架次分段使用不同站 | 第一版逐认证子区段选站，保存结果6个运输架次确实跨两站 | 当前每运输架次只选一个后备站 | 尚未吸收；值得做候选可行域扩展对照 |

## 2. 第一版：实际主路径比函数名显示的更窄

实际链为：`main()`读Q2方案 → `candidates()`读/建缓存 → 固定候选索引85、316、170 → `joint_retime()` → `certified_plan()`逐区段认证 → HiGHS LP → 能源组件事后分配 → 导出。仅在该路径失败时，入口才按候选组合调用通用 `relay_schedule()`。依据：[问题三.py第217行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:217)，运行日志包含“联合重排LP … Optimal”。因此不能把贪心 `relay_schedule()`说成旧正式解的实际主求解算法。

**固定结构LP。** [第149行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:149)固定组批、机型、路线和具体资源顺序，把运输开始时刻、中继服务首末时刻、联合完工作为变量；[第166行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:166)加运输机和电池先后约束，[第174行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:174)加中继到位、服务上限和覆盖窗口，[第190行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:190)加两架中继的固定任务序列。这个结构适合放在大邻域搜索中快速评价一个已经确定资源顺序的方案。

但其目标是“各运输开始时刻的货箱优先系数加权和＋0.1×Cmax＋0.001×中继服务时长”，见[第196行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:196)。它并非当前冻结的多目标函数。若移植LP，只能换成当前固定尺度系数；不能加入新的平均交付优先项后声称同目标改善。

**同站多班与组件复用。** 通用函数[第87行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:87)先合并站点需求区间，只有总服务长度满足能量上限且相邻需求间隙小于900秒才继续合班；随后用两架实体、六组件的最早可用时刻着色。900秒是旧代码的启发式阈值，并非题目规定。实际LP的[第155行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:155)则手工定义5个任务，其中站0和站1重复出勤。保存结果也确有此结构，但5次任务使用5个不同组件，没有实际展示组件复用。组件分配是在LP完成后按充满时刻贪心完成，见[第207行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:207)，不能称为LP内联合优化组件。

**真正遗漏的能力：按轨迹区段换站。** [第62行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:62)对每个认证子区段先尝试直连，否则从可用中继站中选择；各区段可取不同站。保存的 `通信保障.csv` 中，T004、T006、T009、T013、T016、T018确实各使用两个站。当前 [Profiles.get第114行](E:/sxjm/D题/code/solve_q3_senior.py:114)要求一个站覆盖整个候选运输架次，[enrich第366行](E:/sxjm/D题/code/solve_q3_senior.py:366)也据此过滤候选。允许分段后备可能保住当前被排除的跨覆盖区合批路线，这是扩大搜索空间的实际机会。

移植还需去掉旧代码的固定站索引、任务编号和资源顺序。特别是[第186行](D:/Desktop/Agent/第一版/求解/问题三/问题三.py:186)对未匹配区段可能取`j=5`，而`definitions`只有5项；这条备用路径不具备通用健壮性。当前旧输出成功，不代表这些硬编码适用于新候选。

## 3. 111：共享覆盖可借鉴，调度与物理代码必须重写

实际程序在模块顶层直接执行：读取Q2 CSV → 用 `trip()`重算各运输架次 → `coverage()`按45秒样本找单站 → 必要时拆成单服务区 → 先做紧急共享组 → 对剩余任务枚举机体、电池、中继、组件进行截止优先贪心 → 用30秒样本检查通信。入口和拆分见[第69行](D:/Desktop/Agent/111/求解/问题三/问题三.py:69)，剩余调度见[第176行](D:/Desktop/Agent/111/求解/问题三/问题三.py:176)。没有发现一个随后被调用的LP或完整联合CP模型。

**候选补点。** [第28行](D:/Desktop/Agent/111/求解/问题三/问题三.py:28)沿网关到任务群中心的方向取0.28、0.5、0.72、1.0位置，加450/900米横纵偏移，高度160/240/300米。这个任务驱动构造可以补充当前通用500米网格，但所有新点必须使用当前DEM、三维链路、9.80665重力和连续认证重新计算。旧点的通过标志与最小裕量不能直接复用。

**紧急任务共享。** [第124行](D:/Desktop/Agent/111/求解/问题三/问题三.py:124)为3600秒紧急任务构造“站点×运输任务”覆盖掩码，选两个点再形成共享组。代码实际最大化的是覆盖任务数量，不是注释声称的“急需箱数”；随后还用S007硬编码替换第二个站。可借鉴的是任务级覆盖掩码，用作新候选排序或热启动；更合理的新排序应结合实际硬时限余量、紧急箱数和需要中继的区间，而不是复制S007规则。

**新核验发现的通用性问题。** [第155行](D:/Desktop/Agent/111/求解/问题三/问题三.py:155)虽然选择当前`free/ready`最小的机体和电池，但[第156行](D:/Desktop/Agent/111/求解/问题三/问题三.py:156)的早期组开始时刻没有取它们的可用时刻最大值。若共享组同机型任务超过初始实体数量，就可能排出重叠任务；后续断言只能事后发现。这里指出的是源码对一般输入缺乏保证，没有声称保存结果实际发生了该类重叠。

此外，[物理模型.py第95行](D:/Desktop/Agent/111/求解/物理模型.py:95)仍使用线性载荷航程，[第120行](D:/Desktop/Agent/111/求解/物理模型.py:120)把硬截止仅映射到首批字段，同站箱共同完成；这些都与当前模型不同。45/30秒样本不构成连续通信证明。保存结果为29运输＋20中继、11箱医疗/首批超期；虽能看到RE01/RE02/RE03各用6次、RE04用2次，不能继承其可行性或优化指标。

## 4. 用当前21+3结果判断收益，而非只看旧算法名称

对当前各中继实际承担的 `relay=true` 连续保障区间做并集，得到：

| 中继 | 当前站点 | 保障运输架次总数 | 同时保障峰值 | 保障并集内部空隙 | 服务窗多出的边界量化余量 | 返航SOC |
|---|---|---:|---:|---:|---:|---:|
| R001 | H0720 | 2 | 2 | 无 | 0.003333 s | 86.6621% |
| R002 | H0020 | 5 | 4 | 无 | 0.000560 s | 73.7092% |
| R003 | H0003 | 9 | 7 | 无 | 0.005235 s | 62.2069% |

这里的并集是已有连续证书下的后备保障需求，并非声称每一时刻所有运输机都实际通过中继通信。它足以说明：在冻结当前通信分配和运输时刻的条件下，拆中继班次没有可消除的大悬停空档；现三个能源组件也没有触及六组库存。

进一步，在其他指标不变时，当前目标中多一个架次的惩罚相当于增加约0.967919954 kWh能耗。拆班还会增加一次往返＋30秒建链。按三个站当前参数，要仅靠省悬停弥补这些代价，所消除的空隙至少分别约3786.6、4239.1、3957.7秒；当前内部空隙都是0。这个计算只用于冻结其他指标的分班筛查，不排除重新组批或改变站点后出现不同结论。

当前R003于6620.617秒返航，而最后运输T021于6619.643秒返航。若运输时刻完全不动，仅提早末次中继返回最多使联合完工改善0.974秒；要取得明显时间收益，还要改变运输排程或路线组织。

## 5. 已执行的受控LP诊断

为核实“再加固定结构LP能否明显改善”，本轮用当前JSON独立建立28变量、83条线性不等式的HiGHS LP。固定21个货箱组、机型、访问与箱序、3个站及区段归属、机体/电池顺序和中继实体序列；保留所有期望时限及医疗/首批硬时限，沿用当前零迟到对照和原固定尺度目标。航段与充电offset仍取已验证的毫秒值，只有开始/服务窗口变量进入连续LP。

| 指标 | 当前冻结解 | 固定结构LP诊断 |
|---|---:|---:|
| 联合完成/s | 6620.617 | 6620.616 |
| 总能耗/kWh | 66.841159703975 | 66.841142592864 |
| 未取整固定尺度评分 | 0.007079160745 | 0.007078974290 |

评分下降约`1.86455e-7`，小于当前`1e-6`目标量化误差界；时间仅改善1毫秒，能耗仅减少约`1.71111e-5 kWh`。LP中部分非关键任务可以平移约30秒，这是同结构下的时间自由度，不代表联合完工有30秒改善。最大线性约束残差约`4.55e-13 s`。

详细控制条件和约束标签见 [fixed_structure_lp_diagnostic.json](./fixed_structure_lp_diagnostic.json)。这是连续LP的诊断结果，没有替换正式方案，也未作为新毫秒排程交付。它支持的结论是：**仅在当前同一结构上再套LP，收益已经很小；LP的主要价值是为新的结构搜索提供便宜的评价/修复工具。**

## 6. 可直接开展的受控升级

**对照A，优先实施：42站与任务驱动扩展站池。** 固定当前21运输候选、物理参数、零迟到约束和目标，只扩充站池。先从已有1158点中，按当前R001/R002/R003各自保障的运输组，用完整轨迹连续profile筛出共同可覆盖点，各补若干去返程短、服务能耗低或余量较大的非支配候选；必要时加入111方向偏移构造的新点。保留当前42站及现解hint，用相同种子和时间预算比较。记录新增可行点、严格同目标评分、Cmax、总能耗、架次、最小通信裕量与运行时间。该试验复用现模型和验证器，实施成本最低；候选筛选按组进行不代表强制固定这些组的服务关联。

**对照B：单站后备与最多两站分段后备。** 固定同一运输候选集合和42站池，仅放宽当前单站profile过滤。对单站不可覆盖但双站按区段可覆盖的候选保留新选项；每段必须绑定实际中继任务，服务窗完整包含该段，接入和回传同时成立，换站点两侧连续覆盖。每时刻只执行一个后备选择，不能把两个不同时在岗的候选覆盖拼成可行证据。首先统计新增跨区候选数，再在同预算下比较是否实际减少运输架次、能耗或完工。实施时把`station`从架次级推广到区段级；若进一步允许同站多班，另需独立`relay_sortie_id`。本轮父任务已开始增加多站窗口和声明式验证接口，仍须用完整连续验证确认实际输出，不能只放宽站数断言。该能力与第一版保存结果的实际行为一致，但算法和参数应重建。

**对照C，条件成熟再做：每站1次与每站至多2次、组件不复用与可复用。** 将“站点”和“中继任务”分开建索引，组件库存为6组，而非将6组库存替换成最多6次任务。实体占用含300秒周转，组件占用到按SOC分段充电结束。若A/B或更大运输邻域产生了明显分离的服务需求、超长窗口或超过6次出勤，再优先验证该功能。现21+3方案没有这些触发条件。

三组对照都应冻结原输入/参数和基线哈希，保留基线作为可行上界，记录失败或无改善结果。任何新“更优解”须先通过对应独立验证；小于目标量化误差的变化应作为数值精修报告，不作为明显算法提升。
